import React from 'react';
import Navbar from './Shared/Navbar';

const Header = () => {

    return (
        <header>
            <Navbar></Navbar>
        </header>
    );
};

export default Header;